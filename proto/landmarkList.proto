syntax = "proto3";

package hand_tracking;

message LandmarkList {
    message Landmark {
        float x = 1;
        float y = 2;
        float z = 3;
    }
    repeated Landmark landmark = 1;
    bool handedness = 2;
}